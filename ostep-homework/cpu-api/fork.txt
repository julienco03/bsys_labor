2. Bei einer geringen fork_percentage (z.B. 0.1) ist der finale process tree sehr klein, da in fast 
jedem Fall ein exit stattfindet. Daher ist die maximale Höhe des Baumes während der Simulation
sehr gering. Bei einer hphen fork_percentage wie 0.9 ist der finale Baum sehr komplex bzw. tief verwurzelt.

4. Wenn 'C exits' stattfindet, würde ich erwarten, dass die Kinder von C an den Elternknoten von B
angehängt werden. Dies ist jedoch nicht der Fall. Die Kindknoten werden an A, also die Wurzel - angehängt.
Mit dem Flag -R passiert genau das, was ich erwartet hätte. Die Kindknoten werden an B angehängt.

6. Man kann nicht in jedem Fall genau sagen, welche Aktion stattgefunden hat. Eine Ausnahme wäre zum Beispiel,
wenn 'a forks b' stattfindet und anschließend a bspw. c forkt und b bspw. d forkt. Dann ist die Reihenfolge
nicht eindeutig.
